1. Edit /etc/systemd/resolved.conf
2. uncomment (remove the #) the DNS= and DNSStubListener= lines
     DNS=default
     DNSStubListener=no
3. sudo ln -sf /run/systemd/resolve/resolv.conf /etc/resolv.conf


reboot


confirm with sudo lsof -i :53
